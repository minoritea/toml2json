# TOML2JSON
```bash
toml2json converts a TOML formated text to a JSON text.
usage: toml2json [flags] [file]
       If no files are specified, toml2json reads the input text from the standard input.
flags:
       -help
       -h    show the help message
```
